# validations

Includes validation functions to ensure data integrity and enforce business rules.

## Example

```ts
// core/validations/emailValidator.ts
export const isValidEmail = (email: string) => {
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
};
```

---

# 🌐 Portuguese / Português

# validações

Inclui funções de validação para garantir a integridade dos dados e aplicar regras de negócio.

## Exemplo

```ts
// core/validations/emailValidator.ts
export const isValidEmail = (email: string) => {
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
};
```
