# constants

Stores static values that are used throughout the application, such as API endpoints, configuration settings, and predefined enums.

## Example

```ts
// core/constants/appConfig.ts
export const TIMEOUT = 5000

export const SUPPORTED_LANGUAGES = ["en", "pt", "es"] as const
```

---

# 🌐 Portuguese / Português

# constants

Armazena valores estáticos que são usados em toda a aplicação, como endpoints de API, configurações e enumerações predefinidas.

## Exemplo

```ts
// core/constants/appConfig.ts
export const TIMEOUT = 5000

export const SUPPORTED_LANGUAGES = ["en", "pt", "es"] as const
```
